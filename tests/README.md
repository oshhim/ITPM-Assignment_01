# SwiftTranslator Playwright Tests

Automated test suite for https://swifttranslator.com/ using Playwright.

## Prerequisites

- Node.js (LTS recommended)

## Setup

1. Clone the repository:
   ```bash
   git clone <YOUR_REPO_URL>
   cd swifttranslator-tests
   ```
